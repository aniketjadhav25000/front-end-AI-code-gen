// import React from 'react';

// const Tabs = ({ currentTab, setCurrentTab }) => {
//   const tabs = ['Generate', 'History', 'Settings'];

//   return (
//     <div className="flex space-x-4 justify-center bg-gray-800 py-3 text-white">
//       {tabs.map((tab) => (
//         <button
//           key={tab}
//           onClick={() => setCurrentTab(tab)}
//           className={`px-4 py-2 rounded ${currentTab === tab ? 'bg-indigo-500' : 'bg-gray-700 hover:bg-gray-600'}`}
//         >
//           {tab}
//         </button>
//       ))}
//     </div>
//   );
// };

// export default Tabs;
